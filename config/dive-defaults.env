# =============================================================================
# DIVE V3 Default Configuration (SSOT)
# =============================================================================
# Purpose: Single source of truth for all configurable default values
# Usage: Automatically loaded by common.sh during CLI initialization
#        Override any value via environment variable, CLI flag, or
#        config/dive-local.env (user-specific, git-ignored)
# =============================================================================
# Precedence (highest to lowest):
#   1. CLI flags (--domain, --secrets-provider, etc.)
#   2. Environment variables
#   3. .env.hub file
#   4. config/dive-local.env (user overrides, git-ignored)
#   5. config/dive-defaults.env (THIS FILE — project defaults)
# =============================================================================

# ---------------------------------------------------------------------------
# IDENTITY & DOMAIN
# ---------------------------------------------------------------------------

# Base domain for all DIVE services (used to derive Caddy subdomains)
# Override: ./dive --domain myorg.com
DIVE_DEFAULT_DOMAIN=dive25.com

# Default hub instance code (3-letter country code)
# Override: ./dive --instance gbr
DIVE_DEFAULT_INSTANCE=usa

# Default environment
# Override: ./dive --env dev
DIVE_DEFAULT_ENVIRONMENT=local

# Default hub realm name (Keycloak)
HUB_REALM=dive-v3-broker-usa

# Hub API fallback URL (used when Hub not auto-detected)
HUB_FALLBACK_URL=https://usa-api.dive25.com

# ---------------------------------------------------------------------------
# CLOUD PROVIDERS
# ---------------------------------------------------------------------------

# Google Cloud Platform project ID
GCP_PROJECT=dive25

# GCP Pilot VM name
PILOT_VM=dive-v3-pilot

# GCP Pilot zone
PILOT_ZONE=us-east4-c

# AWS region (GovCloud by default)
AWS_REGION=us-gov-east-1

# AWS SSH key pair name
DIVE_AWS_KEY_PAIR=ABeach-SSH-Key

# ---------------------------------------------------------------------------
# SECRETS MANAGEMENT
# ---------------------------------------------------------------------------

# Secrets provider: vault (recommended), gcp, aws, local
# Override: ./dive --secrets-provider aws
SECRETS_PROVIDER=vault

# ---------------------------------------------------------------------------
# DATABASE SEEDING
# ---------------------------------------------------------------------------

# Number of ZTDF resources to seed during hub deploy (Phase 12)
SEED_COUNT=5000

# ---------------------------------------------------------------------------
# SOFTWARE VERSIONS (runtime containers)
# ---------------------------------------------------------------------------

KEYCLOAK_VERSION=26.5.2
NODE_VERSION=20
OPA_VERSION=latest
MONGODB_VERSION=7.0
POSTGRES_VERSION=15

# ---------------------------------------------------------------------------
# BOOTSTRAP TOOL VERSIONS (installed on EC2 instances)
# ---------------------------------------------------------------------------

BOOTSTRAP_NODE_MAJOR=24
BOOTSTRAP_TERRAFORM_VERSION=1.13.4
BOOTSTRAP_VAULT_VERSION=1.21.0
BOOTSTRAP_OPA_VERSION=1.12.3
BOOTSTRAP_YQ_VERSION=v4.44.6
BOOTSTRAP_MKCERT_VERSION=v1.4.4

# =============================================================================
# END OF CONFIGURATION
# =============================================================================
