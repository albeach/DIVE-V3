# =============================================================================
# DIVE V3 - GBR Instance Cloudflare Tunnel Configuration
# =============================================================================
# Generated by generate-instance.sh - Do not edit manually
# Regenerate with: ./scripts/generate-instance.sh GBR
# =============================================================================

tunnel: placeholder-tunnel-id
credentials-file: /etc/cloudflared/credentials.json

ingress:
  # Frontend
  - hostname: gbr-app.dive25.com
    service: https://frontend-gbr:3000
    originRequest:
      noTLSVerify: true
      http2Origin: true

  # Backend API
  - hostname: gbr-api.dive25.com
    service: https://backend-gbr:4000
    originRequest:
      noTLSVerify: true

  # Keycloak IdP
  - hostname: gbr-idp.dive25.com
    service: https://keycloak-gbr:8443
    originRequest:
      noTLSVerify: true

  # KAS
  - hostname: gbr-kas.dive25.com
    service: http://kas-gbr:8080
    originRequest:
      noTLSVerify: true

  # Catch-all
  - service: http_status:404
