# DEU Instance Environment Variables
# CRITICAL: DEU uses prosecurity.biz domain (NOT dive25.com)
# Secrets loaded from GCP Secret Manager via sync-gcp-secrets.sh deu

# Public URLs (browser-side) - prosecurity.biz domain
NEXT_PUBLIC_API_URL=https://deu-api.prosecurity.biz
NEXT_PUBLIC_BACKEND_URL=https://deu-api.prosecurity.biz
NEXT_PUBLIC_KEYCLOAK_URL=https://deu-idp.prosecurity.biz
NEXT_PUBLIC_KEYCLOAK_REALM=dive-v3-broker
NEXT_PUBLIC_APP_NAME="DIVE V3 - Germany"
NEXT_PUBLIC_INSTANCE=DEU
NEXT_PUBLIC_INSTANCE_NAME="Germany"
NEXT_PUBLIC_LOCALE=de-DE

# Keycloak client configuration
KEYCLOAK_CLIENT_ID=dive-v3-client-broker
KEYCLOAK_REALM=dive-v3-broker
# Internal Docker network URL for server-side calls
KEYCLOAK_URL=https://keycloak-deu:8443
# CRITICAL: Issuer must match KC_HOSTNAME for token validation
KEYCLOAK_ISSUER=https://deu-idp.prosecurity.biz/realms/dive-v3-broker

# NextAuth configuration - prosecurity.biz domain
NEXTAUTH_URL=https://deu-app.prosecurity.biz
AUTH_TRUST_HOST=true

# TLS configuration for self-signed certs in dev
NODE_TLS_REJECT_UNAUTHORIZED=0

# Secrets are loaded via environment variables from GCP Secret Manager
# Run: source ./scripts/sync-gcp-secrets.sh deu
# Required env vars: KEYCLOAK_CLIENT_SECRET_DEU, NEXTAUTH_SECRET_DEU, AUTH_SECRET_DEU
