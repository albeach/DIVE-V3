# ===========================================
# DIVE V3 Frontend - Environment Variables
# ===========================================
# This is a TEMPLATE file. Copy to .env.local and fill in actual values.
# NEVER commit .env.local files to git!
#
# Setup: cp .env.local.example .env.local
# Then: Edit .env.local with your actual secrets

# ===========================================
# NextAuth.js Configuration
# ===========================================

# NextAuth secret (generate with: openssl rand -base64 32)
AUTH_SECRET=<GENERATE_WITH_OPENSSL_RAND_BASE64_32>

# Alternative name (some NextAuth versions use this)
NEXTAUTH_SECRET=<SAME_AS_AUTH_SECRET>

# Application URL
NEXTAUTH_URL=http://localhost:3000
AUTH_URL=http://localhost:3000

# ===========================================
# Keycloak Configuration
# ===========================================

# Keycloak URLs
# External URL (browser redirects): http://localhost:8081
# Internal URL (server-side calls): http://keycloak:8080
KEYCLOAK_BASE_URL=http://localhost:8081
KEYCLOAK_URL=http://keycloak:8080

# Keycloak Realm
KEYCLOAK_REALM=dive-v3-broker

# Keycloak Client Credentials
KEYCLOAK_CLIENT_ID=dive-v3-client
KEYCLOAK_CLIENT_SECRET=<COPY_FROM_TERRAFORM_OUTPUT>

# ===========================================
# Backend API Configuration
# ===========================================

# Backend API base URL
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000

# API request timeout (milliseconds)
NEXT_PUBLIC_API_TIMEOUT_MS=30000

# ===========================================
# Application Settings
# ===========================================

# Base URL for absolute links
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Environment
NODE_ENV=development

# ===========================================
# Auth0 Configuration (Optional)
# ===========================================

# Auth0 domain (for MCP integration)
NEXT_PUBLIC_AUTH0_DOMAIN=your-tenant.auth0.com

# Enable Auth0 features in UI
NEXT_PUBLIC_AUTH0_MCP_ENABLED=false

# ===========================================
# Feature Flags (Phase 0)
# ===========================================

# Enable real-time validation in wizard
NEXT_PUBLIC_REALTIME_VALIDATION=true

# Enable metrics display in admin UI
NEXT_PUBLIC_METRICS_ENABLED=true

# ===========================================
# UI Theme (Optional)
# ===========================================

# Tier badge colors
NEXT_PUBLIC_TIER_COLOR_BRONZE=#CD7F32
NEXT_PUBLIC_TIER_COLOR_SILVER=#C0C0C0
NEXT_PUBLIC_TIER_COLOR_GOLD=#FFD700

# ===========================================
# Future Configuration (Phases 1-4)
# ===========================================

# Phase 2: Risk Scoring
# NEXT_PUBLIC_RISK_SCORE_PREVIEW=true
# NEXT_PUBLIC_SHOW_TIER_BADGES=true

# Phase 3: AI Features
# NEXT_PUBLIC_AI_SUGGESTIONS_ENABLED=false

# Phase 3: Status Tracking
# NEXT_PUBLIC_STATUS_TRACKING_ENABLED=true

# ===========================================
# Development Tools
# ===========================================

# Enable Next.js debug mode
# DEBUG=next:*

# Disable telemetry
NEXT_TELEMETRY_DISABLED=1

# ===========================================
# Notes
# ===========================================
#
# Important: Keep .env.local in .gitignore!
# Verify: git status | grep .env.local
# Expected: Nothing (file should be ignored)
#
# Security: Run 'chmod 600 .env.local' to restrict access
#
# Troubleshooting:
# - If variables not loading, restart Next.js dev server
# - Public variables must start with NEXT_PUBLIC_
# - Server-side variables (no prefix) only available in API routes
#
# Reference: https://nextjs.org/docs/app/building-your-application/configuring/environment-variables

