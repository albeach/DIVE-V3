# ============================================
# DIVE V3 Git Ignore
# ============================================

# Root-level markdown files (local notes, not tracked)
/*.md
!README.md

# Root-level shell scripts (one-off fixes, not tracked)
/*.sh

# Documentation files (moved to docs/ - keep local only)
docs/*.md
!docs/README.md
docs/*.txt

# Environment files
.env
.env.*
.env.local
.env*.local
.env.production
.env.development
!.env.example
# Pilot spoke selection cache
.pilot-spoke

# Instance-specific environment files and certificates
instances/*/.env
instances/*/.env.*
instances/*/certs/
instances/*/certs/*.pem
instances/*/certs/*.key
instances/*/certs/*.crt
instances/*/certs/*.bak*
# Temporary instance directories
instances/rou/
instances/tmp*/

# Logs
logs/
!frontend/src/app/api/admin/logs/
!frontend/src/app/admin/logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Testing
coverage/
**/coverage/
test-results/
playwright-report/
.nyc_output/

# Next.js
.next/
out/
build/
dist/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
# next-env.d.ts  # Now tracked in git to avoid runtime permissions issues

# IDE
.vscode/*
!.vscode/settings.json
.idea/
*.swp
*.swo
*.tmp
.claude

# Docker
.docker/

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/crash.log
terraform/crash.*.log
terraform/*.tfvars
terraform/*.tfvars.json
!terraform/terraform.tfvars.example
# SSOT: Track hub.tfvars template (should have empty federation_partners)
!terraform/hub/hub.tfvars
terraform/.terraform.lock.hcl

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
*.temp
*.tmp
/tmp/
temp-ref/
.cache/

# Secrets and keys (CRITICAL - Never commit)
*.key
*.pem
*.p12
*.pfx
*.crt
*.cer
secrets/
.secrets/

# Certificate and key files (all variations)
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx
*.jks
*.p7b
*.p7c

# Certificate backups and temporary files
*.bak
*.bak*
*.backup
*certificate.pem.bak*
*key.pem.bak*
*certificate.crt.bak*
*key.key.bak*

# Timestamped backup files
*.bak.20*
*certificate.pem.bak.20*
*key.pem.bak.20*
*tunnel-credentials.json.bak*
*key.pem.bak.20*

# Files with timestamp patterns
*20[0-9][0-9][0-1][0-9][0-3][0-9]*

# OPA bundles
bundle.tar.gz
policies/.bundle/

# Database backups
# Ignore SQL dumps but not migration files
*.sql
!scripts/migrations/*.sql
*.dump
*.backup

# Generated documentation
api-docs/generated/

# Python (if any scripts)
__pycache__/
*.py[cod]
*$py.class
.Python
venv/
env/

# Keep important empty directories
!backend/logs/.gitkeep
!kas/logs/.gitkeep
!frontend/public/.gitkeep

# Resources for reference (exclude root-level resources directory only)
/resources/

bin/
notes/

# Policy uploads (runtime user uploads, not source code)
backend/policies/uploads/

# Backups (local only)
backups/

# Build artifacts
**/target/classes/
keycloak/extensions/target/

# Terraform plan outputs
terraform/*.txt
terraform/tfplan*

# Personal notes in root
*.txt
!package*.json

# Private notes - not tracked in git
/notes/
/docs/notes/

# Archived directories and files (moved from root during cleanup)
docs/archive/

# Documentation files (keep local only)
docs/*.md
!docs/README.md
docs/**/*.md
!docs/**/README.md

# One-off scripts and test payloads
test-download-simple.js
test-opa-decision.json
scripts/fix-*.js
scripts/fix-*.sh
scripts/clean-*.sh
scripts/set-*.js
scripts/*-theme.js

# Temporary instance flags and state files
instances/*/.initialized
instances/*/.localized
instances/*/.hub-registered
instances/*/.env
instances/*/.env.*

# Instance-generated docker-compose files (regenerated by spoke init)
instances/*/docker-compose.yml

# Certificate signing requests and certificates (generated)
instances/*/certs/*.csr
instances/*/certs/*.crt
instances/*/certs/*.pem
instances/*/certs/*.key

# State files - REMOVED (2026-01-22)
# State is now stored in PostgreSQL only. See ADR-001.
.dive-state/
*.state

# Shared secrets scripts (sensitive)
docker/instances/shared/load-secrets.sh
docker/instances/shared/start-redis.sh

*.bak
*.bak2
# GitHub Actions Runner (installed on home server, not local)
actions-runner/
.env.cloudflare
cloudflared/tunnel-credentials.json

# Secrets - NEVER COMMIT
.env.secrets
.env.secrets.*
!.env.secrets.example

# ============================================
# ARCHIVE - Deprecated configs, scripts, docs
# ============================================
# Contains deprecated items from repository cleanup
# These are kept locally for reference but not tracked
.archive/

# Terraform instance-specific variable files (generated from federation-registry.json)
terraform/instances/*.tfvars
terraform/instances/*.tfplan

# Postgres init scripts (generated)
scripts/postgres-init/
gcp/
.env.gcp

# Terraform generated files
*.plan
*.tfplan

# ============================================
# Terraform Service Account Keys (CRITICAL - DO NOT COMMIT)
# ============================================
.terraform-keys/
# Checkpoints (generated)

# OPAL SSOT: MongoDB is the single source of truth
# Static JSON data files should NOT be committed
policies/data.json
policies/tenant/*/data.json
backend/data/opal/trusted_issuers.json
backend/data/opal/federation_matrix.json
backend/data/opal/tenant_configs.json
opal-data-source/trusted_issuers.json
opal-data-source/federation_matrix.json

# Exception: Keep policy .rego files (policy logic, not data)
!policies/**/*.rego
