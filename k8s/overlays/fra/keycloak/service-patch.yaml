# =============================================================================
# DIVE V3 Keycloak - FRA Service Patch
# NOTE: Kustomize patches must use base resource names, not modified names
# =============================================================================

apiVersion: v1
kind: Service
metadata:
  name: keycloak
spec:
  selector:
    app: keycloak


