# =============================================================================
# DIVE V3 Backend - GBR Service Patch
# NOTE: Kustomize patches must use base resource names, not modified names
# =============================================================================

apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    app: backend

