# =============================================================================
# DIVE V3 OPA - Kubernetes Service
# =============================================================================

apiVersion: v1
kind: Service
metadata:
  name: opa
  namespace: dive-v3
  labels:
    app.kubernetes.io/name: opa
    app.kubernetes.io/part-of: dive-v3
spec:
  type: ClusterIP
  ports:
    - port: 8181
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: opa











