version: '3.8'

networks:
  dive-network:
    driver: bridge

volumes:
  postgres_data:
  mongo_data:
  redis_data:

services:
  # Include federation services if enabled
  # Run with: docker compose -f docker-compose.yml -f docker-compose.federation.yml up
  
  postgres:
    image: postgres:15-alpine
    container_name: dive-v3-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/setup/init-db.sh:/docker-entrypoint-initdb.d/init-db.sh
    networks:
      - dive-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  keycloak:
    build:
      context: ./keycloak
      dockerfile: Dockerfile
    container_name: dive-v3-keycloak
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/keycloak_db
      KC_DB_USERNAME: postgres
      KC_DB_PASSWORD: password
      # Keycloak 26 Configuration with HTTPS (Best Practice)
      # Using self-signed certificate for development
      # Set KC_HOSTNAME for consistent token issuer (critical for federation)
      KC_HOSTNAME: localhost
      KC_HOSTNAME_PORT: 8443
      KC_HOSTNAME_STRICT: false
      KC_HOSTNAME_STRICT_HTTPS: false
      KC_HTTPS_CERTIFICATE_FILE: /opt/keycloak/certs/certificate.pem
      KC_HTTPS_CERTIFICATE_KEY_FILE: /opt/keycloak/certs/key.pem
      KC_HTTP_ENABLED: true
      KC_HTTPS_PORT: 8443
      KC_LOG_LEVEL: info
      KC_METRICS_ENABLED: true
      KC_HEALTH_ENABLED: true
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      # Gap #6: Enable JavaScript providers for script-based protocol mappers
      KC_FEATURES: scripts
      # DIVE Root CA Trust Store - for federation with external DIVE IdPs
      JAVA_OPTS: -Djavax.net.ssl.trustStore=/opt/keycloak/certs/dive-root-cas/dive-truststore.jks -Djavax.net.ssl.trustStorePassword=changeit
    command: start-dev --spi-login-protocol-openid-connect-suppress-logout-confirmation-screen=true --features=scripts
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "8081:8080"   # HTTP (for internal Docker access)
      - "8443:8443"   # HTTPS (for browser and external access)
    networks:
      - dive-network
    volumes:
      - ./keycloak/certs:/opt/keycloak/certs:ro
      # DIVE Root CA certificates for federation trust
      - ./keycloak/certs/dive-root-cas:/opt/keycloak/certs/dive-root-cas:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/realms/master || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  mongo:
    image: mongo:7.0
    container_name: dive-v3-mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: dive-v3
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - dive-network
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: dive-v3-redis
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - dive-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  opa:
    image: openpolicyagent/opa:latest
    container_name: dive-v3-opa
    # Multi-platform: Uses native arch (ARM64 on Mac, AMD64 on Ubuntu)
    # Override with: docker compose -f docker-compose.yml -f docker-compose.platform.yml up
    command:
      - "run"
      - "--server"
      - "--addr=0.0.0.0:8181"
      - "--log-level=info"
      - "--log-format=json"
      - "--set=decision_logs.console=true"
      - "/policies"
    ports:
      - "8181:8181"
    volumes:
      - ./policies:/policies:ro
    networks:
      - dive-network
    healthcheck:
      test: ["CMD-SHELL", "wget --spider --quiet http://localhost:8181/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  authzforce:
    image: authzforce/server:12.0.1
    container_name: dive-v3-authzforce
    ports:
      - "8282:8080"
    volumes:
      - ./authzforce/conf:/opt/authzforce-ce-server/conf:ro
      - ./authzforce/data:/opt/authzforce-ce-server/data
      - ./policies/uploads:/policies:ro
    environment:
      - JAVA_OPTS=-Xms256m -Xmx512m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/authzforce-ce/"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - dive-network
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev  # Development mode with hot reload
    container_name: dive-v3-backend
    environment:
      NODE_ENV: development
      PORT: 4000                      # HTTPS port (all access)
      # Multi-realm migration (Oct 21, 2025): Updated to broker realm with HTTPS
      KEYCLOAK_URL: https://keycloak:8443
      KEYCLOAK_REALM: dive-v3-broker
      KEYCLOAK_CLIENT_ID: dive-v3-client-broker
      KEYCLOAK_CLIENT_SECRET: 8AcfbgtdNIZp3tbrcmc2voiUfxNb8d6L
      KEYCLOAK_JWKS_URI: https://keycloak:8443/realms/dive-v3-broker/protocol/openid-connect/certs
      NODE_TLS_REJECT_UNAUTHORIZED: "0"  # Allow self-signed certs
      MONGODB_URL: mongodb://admin:password@mongo:27017
      MONGODB_DATABASE: dive-v3
      OPA_URL: http://opa:8181
      KAS_URL: http://kas:8080
      AUTHZFORCE_URL: http://authzforce:8080/authzforce-ce
      LOG_LEVEL: info
      # Redis for token blacklist (Gap #7)
      REDIS_URL: redis://redis:6379
      # DIVE Root CA Trust - for federation with external DIVE IdPs
      NODE_EXTRA_CA_CERTS: /app/certs/dive-root-cas/dive-root-cas.pem
    ports:
      - "4000:4000"    # HTTPS (all access)
    depends_on:
      - keycloak
      - mongo
      - opa
      - authzforce
    networks:
      - dive-network
    volumes:
      # Development: Mount source code for hot reload
      - ./backend/src:/app/src
      - ./backend/package.json:/app/package.json:ro
      - ./backend/tsconfig.json:/app/tsconfig.json:ro
      # Mount SSL certificates for HTTPS server
      - ./keycloak/certs:/opt/keycloak/certs:ro
      # CRITICAL: Mount policies directory so backend can read OPA Rego files
      - ./policies:/app/policies:ro
      # CRITICAL: Mount certs directory for X.509 PKI certificates
      - ./backend/certs:/app/certs:ro
      # DIVE Root CA certificates for federation trust
      - ./backend/certs/dive-root-cas:/app/certs/dive-root-cas:ro
      # Logs directory
      - ./backend/logs:/app/logs
      # IdP Management Revamp: Theme uploads directory
      - ./backend/uploads:/app/uploads
      # Policies Lab: Upload directory for user policies
      - ./policies/uploads:/app/policies/uploads

  nextjs:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev  # Development mode with hot reload
    container_name: dive-v3-frontend
    environment:
      NODE_ENV: development
      # BEST PRACTICE: Pure Docker networking (no extra_hosts)
      # Browser (NEXT_PUBLIC_*): Uses localhost (exposed ports)
      # Server-side: Uses Docker service names (internal network)
      NEXT_PUBLIC_API_URL: https://localhost:4000
      NEXT_PUBLIC_BACKEND_URL: https://localhost:4000
      NEXT_PUBLIC_BASE_URL: https://localhost:3000
      # Server-side backend URL (Docker internal network)
      # Uses HTTPS with self-signed certs (NODE_TLS_REJECT_UNAUTHORIZED=0)
      BACKEND_URL: https://backend:4000
      # Multi-realm migration (Oct 21, 2025): Updated to broker realm
      # Server-side NextAuth: Use keycloak:8443 (Docker network for container-to-container with HTTPS)
      # NextAuth running inside the container needs to reach Keycloak via Docker network
      KEYCLOAK_BASE_URL: https://keycloak:8443
      KEYCLOAK_URL: https://keycloak:8443
      KEYCLOAK_REALM: dive-v3-broker
      # Option 3: Custom Theme - SSL enabled
      AUTH_TRUST_HOST: "true"
      NODE_TLS_REJECT_UNAUTHORIZED: "0"  # Allow self-signed certs in development
      KEYCLOAK_CLIENT_ID: dive-v3-client-broker
      KEYCLOAK_CLIENT_SECRET: 8AcfbgtdNIZp3tbrcmc2voiUfxNb8d6L
      # Public Keycloak URLs (for browser-side logout with HTTPS)
      NEXT_PUBLIC_KEYCLOAK_URL: https://localhost:8443
      NEXT_PUBLIC_KEYCLOAK_REALM: dive-v3-broker
      # NextAuth configuration
      AUTH_URL: https://localhost:3000
      AUTH_SECRET: fWBbrGVdA46YMp+7ZB125SXcTp6nA+mxic2KRzKg7sg=
      NEXTAUTH_URL: https://localhost:3000
      # Database for NextAuth sessions
      DATABASE_URL: postgresql://postgres:password@postgres:5432/dive_v3_app
      # DIVE Root CA Trust - for server-side federation requests
      NODE_EXTRA_CA_CERTS: /app/certs/dive-root-cas/dive-root-cas.pem
    ports:
      - "3000:3000"
    depends_on:
      - keycloak
      - backend
    networks:
      - dive-network
    volumes:
      # Development: Mount entire source for hot reload
      - ./frontend:/app
      # Mount SSL certificates for HTTPS server
      - ./keycloak/certs:/opt/keycloak/certs:ro
      # DIVE Root CA certificates for federation trust
      - ./frontend/certs/dive-root-cas:/app/certs/dive-root-cas:ro
      # Exclude node_modules and .next from host (use container's versions)
      - /app/node_modules
      - /app/.next

  kas:
    build:
      context: ./kas
      dockerfile: Dockerfile
    container_name: dive-v3-kas
    environment:
      NODE_ENV: development
      KAS_PORT: 8080
      BACKEND_URL: https://host.docker.internal:4000
      NODE_TLS_REJECT_UNAUTHORIZED: "0"  # Allow self-signed certs for HTTPS backend
      OPA_URL: http://opa:8181
      MONGODB_URL: mongodb://admin:password@mongo:27017
      MONGODB_DATABASE: dive-v3
      LOG_LEVEL: info
      # Multi-realm migration (Oct 21, 2025): KAS needs Keycloak config for JWT validation
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_REALM: dive-v3-broker
      KEYCLOAK_CLIENT_ID: dive-v3-broker-client  # Phase 2.1: Correct client name for national realms
      # Backend should be internal Docker network when fully containerized
      # Change to http://backend:4000 when running backend in Docker
      # Currently: http://host.docker.internal:4000 (backend runs outside Docker)
      # DIVE Root CA Trust - for federation with external DIVE IdPs
      NODE_EXTRA_CA_CERTS: /app/certs/dive-root-cas/dive-root-cas.pem
    ports:
      - "8080:8080"
    depends_on:
      - opa
      - mongo
    networks:
      - dive-network
    volumes:
      - ./kas/src:/app/src
      - ./kas/logs:/app/logs
      # DIVE Root CA certificates for federation trust
      - ./kas/certs/dive-root-cas:/app/certs/dive-root-cas:ro
    command: npm run dev

